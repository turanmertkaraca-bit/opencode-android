<?xml version="1.0" encoding="utf-8"?>
<!-- IDs used by programmatic views (ProviderSetupActivity). -->
<resources>
    <item name="psuRoot" type="id"/>
    <item name="psuList" type="id"/>
    <!-- P33: the decor tag carrying the palette id a screen was built with
         (Theme.stampApplied / appliedIdOf — the per-screen stale check). -->
    <item name="oc_palette_applied" type="id"/>
</resources>
