<?xml version="1.0" encoding="utf-8"?>
<!-- P9: stop button — dark fill, red rim. P42-check: the rim reads the
     palette's err color (the OLED-era hex ignored the active palette). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="#3A1A22"/>
    <stroke android:width="2dp" android:color="@color/err"/>
</shape>
